# Tasks

1. Create a type alias that is generated based on a list of strings
2. Create a function that you read the contents of a file
    a. With a parameter that is generated based on files availlable in a directory
    b. That indicates None for invalid files, but another type for valid ones
3. Create a way to run a command to generate types only when needed, instead of every import/run
